#include "mylib.h"

int main() {
    string fpav;
    vector<Studentas> Grupe;
    int s;
    cout << "Ar norite sukurti atsitiktinius studentu sąrašus? Taip - (1), Ne - (2): ";
    cin >> s;
    if(s==1){
        StudentuSarG();
    }
    cout << "Iveskite failo pavadinima: ";
    cin >> fpav;
    SkaitytiIsFailo(fpav, Grupe);
    
    std::sort(Grupe.begin(), Grupe.end(), [](const Studentas& a, const Studentas& b) {
        if (a.pav == b.pav)
            return a.var < b.var;
        return a.pav < b.pav;
    });
    
    IrasytiIFaila("Rezultatai1000A.txt", Grupe, "nuskriaustukai");
    IrasytiIFaila("Rezultatai1000B.txt", Grupe, "kietuoliai");
    return 0;
    
}
