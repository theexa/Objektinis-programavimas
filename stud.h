#pragma once

#include <string>
#include <vector>

struct Studentas {
    std::string var;
    std::string pav;
    std::vector<int> paz;
    int egz;
    double gal;
    double med;
    std::string gr;
};
